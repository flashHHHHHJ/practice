const path = require('path')

let str = '/root/a/b/1.txt'

// console.log(path.dirname(str)) //得到路径

// console.log(path.extname(str)) //得到原始文件的扩展名

// console.log(path.basename(str)) //得到文件名（包括扩展）

// path.resolve() //将多个混乱地址解析为一个

// console.log(path.resolve(__dirname, 'build'))